import XCTest
@testable import BLAS_LAPACK_AppStore_Workaround

final class BLAS_LAPACK_AppStore_WorkaroundTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BLAS_LAPACK_AppStore_Workaround().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
